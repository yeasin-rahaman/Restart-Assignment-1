1) What is the difference between null and undefined?
   ans: Null means there is no data and undefined there is data but its not define yet 
3) What is the use of the map() function in JavaScript? How is it different from forEach()?
   ans: map() run the each  element and return a new array forEach() run each element dose it work and dosent return any new array
5) What is the difference between == and ===?
   ans: == checks for value === checks for value and type
7) What is the significance of async/await in fetching API data?
   ans: async/await is use to run a function without wait if a data is taking time to load it will wait for the data to load then go to the next step so there is no errors
9) Explain the concept of Scope in JavaScript (Global, Function, Block).
    ans: Global value can access from everywhare, function value can be access only inside a function and block scope is blocked inside a function
